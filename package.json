{
  "name": "create-or-find-terraform-workspace",
  "version": "0.1.3",
  "description": "Create a terraform workspace if it does not exist. Otherwise, find the ID.",
  "main": "index.js",
  "author": "Hans-Christian Otto <c.otto@suora.com>",
  "license": "MIT",
  "scripts": {
    "format": "prettier --write package.json index.js"
  },
  "dependencies": {
    "@actions/core": "^1.2.4",
    "axios": "^0.19.2"
  },
  "devDependencies": {
    "prettier": "^2.0.5",
    "standard-version": "^8.0.0"
  }
}
